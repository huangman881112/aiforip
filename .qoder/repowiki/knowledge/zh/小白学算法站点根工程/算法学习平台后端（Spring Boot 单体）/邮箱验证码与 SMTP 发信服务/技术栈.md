Spring Boot `@ConfigurationProperties` + `jakarta.mail` / `org.springframework.mail.javamail.JavaMailSenderImpl` 直连 SMTP（非 Spring `MailSender` 自动装配），通过 `mail.smtp.ssl.trust=*` 兼容 QQ/163 等自签或 STARTTLS 升级场景。