Spring Boot 3.2 + Java 17；SQLite（jdbc:sqlite:data/suanfa.db，纯 JdbcTemplate 直写 SQL）；MongoDB（spring-boot-starter-data-mongodb）存储算法详情文档；JWT 鉴权使用 jjwt 0.11.5（HS256）；密码哈希使用 Spring Security Crypto BCrypt；AI 对话通过 JDK HttpClient 以 SSE 流式转发 OpenAI 兼容上游。