直接在浏览器中打开 `archive/index.html` 即可访问主站；打开 `archive/log-viewer.html` 可查看由 `log-console.js` 写入 `localStorage.consoleLogs` 的控制台日志；打开 `binary-search-test.html` 后在开发者控制台中执行 `final-binary-search-test.js` 可诊断二分查找数据是否已正确注入到 `window.algorithmData.searching`。