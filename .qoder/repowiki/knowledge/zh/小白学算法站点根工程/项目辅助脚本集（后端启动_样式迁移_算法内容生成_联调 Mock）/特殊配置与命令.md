`scripts/dev-backend.sh [--port PORT] [其他 --KEY=VALUE]` 需先复制 `backend/.env.example` 为 `backend/.env` 填入 AI 中转站 token；日志默认输出到 `/tmp/suanfa-backend-<PORT>.log`。`scripts/dark_theme_codemod.py` 直接改写 `suanfa_vue/src` 下文件，建议先在 git 工作区外试运行。`scripts/import-videos.mjs` 会覆盖 `backend/src/main/resources/seed/algorithms-content.json` 并生成 `/tmp/opencode/update-videos.js` 供 MongoDB 导入。