- 每个功能页面采用自包含的 HTML 单文件结构，样式与脚本内联或通过 CDN 引入，不依赖外部构建流程。
- 调试脚本统一通过读取 `window.algorithmData` 全局对象来校验算法数据结构，并以分段的 `console.log` 输出定位问题。
- 日志查看器将每条日志封装为含 `timestamp`、`type`、`message` 的对象数组，以 JSON 形式序列化后存入 `localStorage` 的 `consoleLogs` 键。