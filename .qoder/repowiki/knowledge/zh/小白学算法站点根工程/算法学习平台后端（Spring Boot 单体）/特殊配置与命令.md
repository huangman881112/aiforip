启动前需确保 `data/` 目录存在（`SuanfaApplication.main` 会自动创建），SQLite 数据库文件为 `data/suanfa.db`；生产环境必须通过环境变量覆盖 `suanfa.jwt.secret`、`suanfa.cors.allowed-origins`、`AI_BASE_URL`、`AI_API_KEY` 等敏感配置；种子数据位于 `resources/seed/algorithms.json` 与 `algorithms-content.json`，首次启动由 `DataInitializer` 自动导入。