根级通过 scripts 统一入口：`mvn spring-boot:run` 启动后端（8080 端口），Vite dev server 通过 `/api` 代理转发到后端；scripts 还提供 `node scripts/generate-algorithms.js` 从前端 `src/data/algorithms.js` 同步种子数据到数据库，以及 `node scripts/mock-openai.js` 进行 AI 联调。